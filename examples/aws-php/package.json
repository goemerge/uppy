{
  "name": "@growthcloud-example/aws-php",
  "version": "0.0.0",
  "dependencies": {
    "@growthcloud/aws-s3": "workspace:*",
    "@growthcloud/core": "workspace:*",
    "@growthcloud/dashboard": "workspace:*",
    "uppy": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.15.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "start": "php -S localhost:8080 serve.php",
    "outputBundle": "esbuild --format=esm --sourcemap=inline --bundle ./main.js"
  }
}
